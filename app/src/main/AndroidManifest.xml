<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBankMate"
        tools:targetApi="31">
        <activity
            android:name=".MoreActivity"
            android:exported="false" />
        <activity
            android:name=".MoveMoneyActivity"
            android:exported="false" />
        <activity
            android:name=".AccountsActivity"
            android:exported="false" /> <!-- Splash Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" /> <!-- Reset Password Activity -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="false" /> <!-- Admin Activity -->
        <activity
            android:name=".AdminActivity"
            android:exported="false" /> <!-- User Management Activity -->
        <activity
            android:name=".UserManagementActivity"
            android:exported="false" /> <!-- Admin Management Activity -->
        <activity
            android:name=".AdminManagementActivity"
            android:exported="false" /> <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
    </application>

</manifest>