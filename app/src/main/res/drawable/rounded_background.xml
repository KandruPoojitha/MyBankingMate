<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <solid android:color="@color/bottom_nav_item_color" />

    <!-- Rounded corners -->
    <corners android:radius="16dp" />

    <!-- Padding inside the background -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
